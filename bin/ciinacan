#!/usr/bin/env ruby

require 'ci_in_a_can'

case ARGV[0].to_s.downcase
when 'create'

  root = "#{Dir.pwd}/#{ARGV[1]}"
  id   = root.split('/').pop

  CiInACan::Cli::StructureBuilder.new(root, id).create

end
